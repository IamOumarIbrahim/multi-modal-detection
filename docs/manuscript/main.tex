%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% MMSAR Conference Paper (ICSPIS 2026)
% IEEE Conference Template (Two-Column, Strictly <= 6 Pages)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{url}
\usepackage{balance}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc,fit,backgrounds,decorations.pathreplacing}
\setlength{\textfloatsep}{7pt plus 1pt minus 1pt}
\setlength{\floatsep}{6pt plus 1pt minus 1pt}
\setlength{\intextsep}{6pt plus 1pt minus 1pt}
\setlength{\abovecaptionskip}{3pt plus 1pt minus 1pt}
\setlength{\belowcaptionskip}{2pt plus 1pt minus 1pt}

\begin{document}

\title{Lightweight Multimodal Person Detection and Causal Temporal Decision Processing for Aerial Search and Rescue}

\author{\IEEEauthorblockN{Oumar Mamoun Ibrahim and Mohamad Khairi bin Ishak}
\IEEEauthorblockA{\textit{Department of Computer Engineering, University of Sharjah}, Sharjah, United Arab Emirates\\
\{U22200741, mishak\}@sharjah.ac.ae}
}

\maketitle

\begingroup
\renewcommand\thefootnote{}
\footnotetext{This work involved human subject detection in simulated imagery. The author(s) confirm(s) that all human/animal subject research procedures and protocols are exempt from review board approval.}
\endgroup

\begin{abstract}
Autonomous unmanned aerial vehicles (UAVs) in wilderness search and rescue (SAR) require reliable person detection under strict size, weight, power, and computing (SWaP-C) limits. Combining visible red-green-blue (RGB) and thermal infrared (TIR) cameras prevents sensor-specific dropouts caused by canopy shadows, darkness, or thermal crossover. However, per-frame object detectors often produce momentary false alarms from terrain clutter. During autonomous flight, each false alarm triggers unnecessary loiter maneuvers, depletes battery reserves, and wastes telemetry bandwidth over constrained links. This paper evaluates lightweight causal temporal decision methods to generate reliable search alerts from multimodal aerial detections. Using a controlled multimodal benchmark spanning three biomes (arid desert, temperate forest, and snow alpine) across synchronized RGB and pseudo-TIR streams, we deploy two upstream You Only Look Once (YOLO11n) detectors (2.6M parameters, 6.5 giga floating-point operations [GFLOPs]) held strictly fixed. Confidences are combined through a late fusion max gate and filtered over a causal sliding window ($W = 5$ frames, 208~ms latency bound at 24~Hz). We compare six temporal decision strategies with validation-calibrated thresholds: raw thresholding, moving average, median filtering, history consensus, a learned Mamba selective state-space model (SSSM), and a gated recurrent unit (GRU) baseline. On alert-level metrics, a five-frame moving average achieves 100.0\% positive target recall and reduces false alerts from 12.0 to 4.0 false alerts per minute (FA/min) (a 67\% reduction in mission alert events), corresponding to an 87.0\% frame-level false alarm suppression ratio (FASR). It runs in 0.30~$\mu$s per frame with a 20-byte buffer, outperforming raw thresholding, recurrent gating, and state-space modeling in edge efficiency.
\end{abstract}

\begin{IEEEkeywords}
Multimodal fusion, search and rescue, unmanned aerial vehicles, state-space models, temporal filtering.
\end{IEEEkeywords}

\begin{figure*}[!t]
\centering
\resizebox{1.0\textwidth}{!}{\input{figure_system_architecture.tex}}
\caption{System architecture of the MMSAR evaluation framework. Visible RGB video streams are synthesized from scenario text prompts using multimodal large language models (Google Gemini 3.8 Flash and xAI Grok with spatial upscaling to $1280 \times 720$). A 256-level lookup table (\texttt{melo\_lut}) transfers pseudo-thermal styling to emulate synchronized thermal infrared streams. Dual streams undergo $2\times$ square tiling ($640 \times 640$), video-level bounding box annotation in Label Studio, and 24~Hz frame extraction. Curated sequences are partitioned into 240-frame episodes under Option B (parent-video grouped split) with non-adjacent shuffling to prevent spatial data leakage. At edge inference, parallel lightweight YOLO11n detectors process incoming RGB and thermal frames at 24~Hz ($T_s \approx 41.7$~ms), combined with a late fusion max gate ($s[n] = \max(c_{\text{RGB}}[n], c_{\text{Thermal}}[n])$) and evaluated across six lightweight temporal decision methods ($W = 5$ frames, 208~ms latency bound).}
\label{fig:system_architecture}
\end{figure*}

\section{Introduction}

Autonomous unmanned aerial vehicles (UAVs) provide rapid aerial coverage in wilderness search and rescue (SAR) missions \cite{rudol2008human}, \cite{silvagni2017multipurpose}. However, onboard vision systems must operate under strict size, weight, power, and computing (SWaP-C) limits. Remote missions depend on low-bandwidth satellite or cellular Internet of Things (IoT) telemetry \cite{kyrkou2021emergencynet}. In autonomous operations, false alarms trigger costly hover-and-verify maneuvers that drain battery power and risk dispatching rescue teams to empty locations.

Wilderness optical sensing suffers from sensor-specific failure modes. Visible red-green-blue (RGB) cameras degrade in dense shadows, heavy foliage, and poor lighting. Thermal infrared (TIR) cameras detect body heat regardless of illumination \cite{hwang2015multispectral}, \cite{jia2021llvip}, but suffer from thermal crossover when ground temperatures match human body temperature \cite{portmann2014people}. In desert environments, sun-heated rocks emit thermal patterns that mimic human signatures. In snowy terrain, bright reflective snow causes visible glare, while insulated winter clothing reduces body heat emission. Combining RGB and thermal sensors is therefore necessary for reliable target detection across diverse biomes.

Small UAVs require compact single-stage detectors such as You Only Look Once (YOLO) \cite{redmon2016you}. In this work, we deploy Ultralytics YOLO11n \cite{jocher2024yolo11} (2.6M parameters, 6.5 giga floating-point operations [GFLOPs] at $640 \times 640$) trained in 32-bit floating-point (FP32) precision. However, per-frame detections fluctuate due to platform vibration, motion blur, and background clutter. These fluctuations create brief false alarms in background areas and intermittent dropouts on actual human targets.

To address these issues, we evaluate lightweight causal temporal decision methods that convert noisy per-frame multimodal detections into dependable mission alerts. Upstream detectors and late fusion remain strictly fixed across all experiments to isolate the impact of temporal filtering. Frame detections are combined using a late fusion max gate and filtered across a causal five-frame sliding window ($208\text{ ms}$ at $24\text{ Hz}$). We benchmark six temporal decision strategies under validation-calibrated thresholds: raw thresholding, moving average, median filtering, history consensus, a learned Mamba selective state-space model (SSSM) \cite{gu2023mamba}, and a learned gated recurrent unit (GRU) baseline.

The central research question is: \textit{How do lightweight causal temporal decision methods compare in generating reliable alerts from multimodal detections for autonomous aerial search and rescue?}

This work provides three main contributions:
\begin{enumerate}
\item \textbf{Multimodal SAR Benchmark:} A controlled $3\times 2$ experimental setup covering desert, forest, and snow alpine biomes across synchronized RGB and pseudo-TIR video streams, evaluating fixed lightweight YOLO11n detectors with late fusion (Table~\ref{tab:system_config}, Table~\ref{tab:fusion_comparison}).
\item \textbf{Causal Decision Benchmark:} Empirical evaluation of six causal decision filters (raw thresholding, moving average, median filter, history consensus, Mamba-SSSM, and GRU) on alert-level metrics, multi-seed variance, and statistical significance (Table~\ref{tab:postprocessing_benchmark}, Fig.~\ref{fig:real_temporal_sequence}).
\item \textbf{Window Sensitivity and Environmental Breakdown:} Empirical ablation of causal window length ($W \in \{1, 3, 5, 7\}$) and per-biome false alert characterization across desert, forest, and snow terrain, demonstrating that finite impulse response (FIR) moving averages achieve the optimal operational trade-off between false alarm rejection and real-time responsiveness (Table~\ref{tab:per_biome_benchmark}, Fig.~\ref{fig:window_ablation}).
\end{enumerate}

\section{Related Work}
\label{sec:related_work}

Multimodal aerial vision combines complementary visible and thermal bands to mitigate environmental extremes, as explored in ground datasets such as KAIST \cite{hwang2015multispectral} and LLVIP \cite{jia2021llvip}. For wilderness search and rescue (SAR), WiSARD \cite{broyles2022wisard} provides multi-viewpoint benchmarks, while Rudol and Doherty \cite{rudol2008human} demonstrated dual-band geolocation from unmanned aerial vehicles (UAVs). However, edge UAV deployment is tightly constrained by size, weight, power, and computing (SWaP-C) limits \cite{silvagni2017multipurpose}, where complex neural ensembles rapidly drain battery reserves \cite{lygouras2019unsupervised}. Single-stage detectors such as YOLO11n \cite{jocher2024yolo11} deliver real-time frame rates, but per-frame detections fluctuate due to motion blur and terrain thermal crossover \cite{portmann2014people}, generating false alarms that trigger wasteful hover maneuvers.

Classical sequential decision making addresses temporal fluctuations through statistical tests such as Wald's sequential probability ratio test \cite{wald1945sequential}, radar sliding-window binary integration ($M$-of-$N$) \cite{richards2014fundamentals}, and running median or moving average filters \cite{rabiner1975applications}. More recently, selective state-space models such as Mamba \cite{gu2023mamba} provide linear-time sequence modeling with recurrent state representations. While prior SAR literature emphasizes per-frame bounding box accuracy or spatial tracking, the problem of turning noisy multimodal scalar confidence streams into reliable mission-level alerts under strict edge latency budgets remains uncharacterized. This paper addresses this gap by benchmarking causal temporal decision rules on fixed multimodal detector streams.

\section{Methodology}
\label{sec:methodology}

The MMSAR pipeline integrates dual-spectrum detection, late fusion, and causal temporal filtering (Fig.~\ref{fig:system_architecture}). Synchronized visible RGB frames $I_{\text{RGB}}[n]$ and thermal frames $I_{\text{Thermal}}[n]$ produce per-frame target confidences $c_{\text{RGB}}[n], c_{\text{Thermal}}[n] \in [0.0, 1.0]$. The upstream detectors and late fusion gate remain fixed across all experiments.

\subsection{Video Ingestion and Real-Time Edge Budget}
Autonomous UAV companion computers require frame-synchronous processing that matches sensor capture rates. Video streams are captured at $f_s = 24\text{ Hz}$, yielding a frame period $T_s \approx 41.7\text{ ms}$. To avoid dropping frames or accumulating queue delay, processing must satisfy:
\begin{equation}
T_{\text{detect}} + T_{\text{fusion}} + T_{\text{filter}} \le 41.7\text{ ms},
\label{eq:realtime_budget}
\end{equation}
where $T_{\text{detect}} = \max(T_{\text{RGB}}, T_{\text{Thermal}})$ is parallel detection runtime, $T_{\text{fusion}}$ is fusion latency, and $T_{\text{filter}}$ is post-processing latency. We deploy two parallel YOLO11n models \cite{jocher2024yolo11}: (1)~\textbf{YOLO11n-RGB} and (2)~\textbf{YOLO11n-Thermal}. Frames containing no target predictions yield confidence 0.0.

\subsection{Late Fusion Max Gate}
To prevent target dropouts when one sensor is occluded, detector confidences are combined using a late fusion max gate:
\begin{equation}
s[n] = \max\big(c_{\text{RGB}}[n], \, c_{\text{Thermal}}[n]\big) \in [0.0, 1.0].
\label{eq:late_fusion}
\end{equation}
This max gate ensures that confident detections in either spectrum propagate forward without requiring pixel-level sensor registration.

\subsection{Causal Temporal Post-Processing Methods}
\label{sec:causal_postprocessing}
Direct thresholding of raw per-frame confidence produces high false-alarm rates due to brief clutter spikes. Downstream processing buffers the fused confidence stream across a causal sliding window of length $W = 5$ frames ($208\text{ ms}$ at $24\text{ Hz}$): $\mathbf{w}[n] = [s[n-4], \dots, s[n]]$. Each method uses an optimal decision threshold $\tau^* = \arg\max_\tau F_1(\tau)$ calibrated on the validation set $\mathcal{D}_{\text{val}}$ across candidate thresholds from 0.05 to 0.95 (step size 0.02) and held frozen during testing.

We evaluate six causal decision strategies:
\textbf{M1 (Baseline Raw):} Instantaneous thresholding setting $y[n] = 1$ when $s[n] \ge \tau^*$ ($O(1)$ time, 0~B buffer).
\textbf{M2 (Moving Average):} Causal FIR mean $\bar{s}[n] = \frac{1}{W} \sum_{k=0}^{W-1} s[n-k]$, triggering an alert when $\bar{s}[n] \ge \tau^*$ ($O(1)$ time, 20~B buffer).
\textbf{M3 (Median Filter):} Order-statistic median $\tilde{s}[n] = \text{median}(\mathbf{w}[n])$, rejecting noise pulses shorter than 3 frames ($O(W)$ time, 20~B buffer).
\textbf{M4 (History Consensus):} Binary voting requiring at least 3 detections out of 5 frames ($O(W)$ time, 5~B buffer).
\textbf{M5 (Learned Mamba-SSSM):} Pure-PyTorch selective state-space model ($d_{\text{model}} = 16, d_{\text{state}} = 8$, 753 parameters, 512~B state buffer) updating a recurrent state from scalar confidences.
\textbf{M6 (Learned GRU Baseline):} Single-layer gated recurrent unit ($d_{\text{hidden}} = 16$, 1,681 parameters, 64~B state buffer) to benchmark whether conventional recurrent architectures capture temporal dependencies beyond linear low-pass filtering. Both learned models are trained on validation sequences across five random seeds using binary cross-entropy.

\subsection{Alert-Level Evaluation Metrics}
To measure operational mission effectiveness, we report primary alert-level metrics:
\begin{itemize}
\item \textbf{False Alerts per Minute (FA/min):} Rate of false alarm events triggered during non-target periods: $\text{FA/min} = K_{\text{FA}} / T_{\text{flight}}$, where an alarm event is defined as a contiguous sequence of positive detections ($y[n]=1$).
\item \textbf{Positive Target Recall (\%):} Proportion of ground-truth target episodes successfully detected by at least one confirmed alert event.
\item \textbf{Alert Latency (Time-to-Alarm):} Elapsed time (in frames and milliseconds) from target onset to the first triggered alert.
\item \textbf{False Alarm Suppression Ratio (FASR):} Percentage reduction in false alarm frames compared to the raw baseline:
\begin{equation}
\text{FASR} = 1 - \frac{\text{FP}_{\text{filter}}}{\text{FP}_{\text{raw}}},
\label{eq:fasr}
\end{equation}
where $\text{FP}_{\text{filter}}$ and $\text{FP}_{\text{raw}}$ are the total false-positive frames produced by the temporal filter and raw baseline, respectively.
\end{itemize}

\section{Experimental Setup and Dataset}
\label{sec:experimental_setup}

Wilderness SAR dual-spectrum datasets remain scarce in the literature. As summarized in Table~\ref{tab:system_config}, our controlled $3\times 2$ experimental design balances modality and environment across three wilderness biomes:
\begin{itemize}
\item \textbf{Arid Desert:} Rocky terrain, sparse vegetation, and midday thermal crossover, where sun-heated rocks match human skin temperature.
\item \textbf{Temperate Forest:} Dense canopies, dappled shadows, intermittent foliage occlusions, and wildlife distractors.
\item \textbf{Snow-Covered Alpine:} High-albedo snow causing visible glare, contrasted against freezing backgrounds where winter clothing insulates body heat.
\end{itemize}


\begin{table}[!t]
\centering
\caption{Corpus Partitioning and Experimental System Specifications}
\label{tab:system_config}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{ll}
\toprule
\textbf{System Parameter} & \textbf{Design Specification / Value} \\
\midrule
Environments & Arid Desert (30 tiles), Temperate Forest (28 tiles), \\
             & Snow-Covered Alpine (32 tiles) \\
Sensor Modalities & Synchronized visible RGB and pseudo-thermal infrared (TIR) \\
Raw Video Format & $1280 \times 720$ at 24~FPS ($T_s \approx 41.7\text{ ms}$), 10.0~s nominal \\
Dual-Crop Tiling & $2\times$ square tiles ($640 \times 640$, left/right, $y = 40$) \\
Corpus Volume & 90 tiles, 20,364 frames per modality (40,728 total frames; \\
              & 42 positive tiles [5,620 frames], 48 negative distractor tiles [14,744 frames]) \\
Episodic Partition & Option B parent-grouped (realized: 44 train [17 pos, 27 neg], \\
                   & 22 val [12 pos, 10 neg], 24 test [13 pos, 11 neg] tiles per modality) \\
Upstream Detectors & Dual YOLO11n (2.6M params, 6.5 GFLOPs at $640\times 640$, FP32) \\
Training Setup & Batch 16, 60 epochs, SGD cosine schedule ($\text{lr}_0=0.01$) \\
Decision Window & Sliding causal window $W \in \{1, 3, 5, 7\}$ ($W=5$ nominal; 208~ms) \\
Decision Strategies & Raw (M1), Moving Avg (M2), Median (M3), Consensus (M4), \\
                    & Mamba-SSSM (M5), GRU Baseline (M6) \\
\bottomrule
\end{tabular}%
}
\end{table}

Raw aerial video is captured in $1280 \times 720$ resolution at 24 frames per second (FPS). To preserve ground sampling distance (GSD) without downsampling small human targets, a $2\times$ dual-crop extraction produces disjoint $640 \times 640$ square tiles (left tile $x \in [0, 640]$ and right tile $x \in [640, 1280]$, $y = 40$). Because target persons appear within a single spatial tile in positive parent captures while the companion tile captures ambient terrain, classifying tiles by bounding box presence yields 30 desert tiles (13 positive, 17 negative), 28 forest tiles (14 positive, 14 negative), and 32 snow alpine tiles (15 positive, 17 negative). Across the 90 tiles per modality (42 positive, 48 negative distractor tiles; 180 synchronized streams), the corpus comprises 20,364 frames at 24~Hz per modality (40,728 frames total: 5,620 target frames and 14,744 negative distractor frames per modality). Sequences are annotated in Label Studio using keyframe interpolation, yielding precise ground-truth bounding boxes.

Dataset partitioning is conducted strictly at the parent-video level under Option B (Parent-Video Grouped Episodic Partitioning) to prevent spatial or temporal data leakage between derived sibling tiles. Sibling left and right tiles are assigned to the same partition. The realized partition allocates 44 training tiles (17 positive, 27 negative; 9,800 frames per modality), 22 validation tiles (12 positive, 10 negative; 4,994 frames per modality), and 24 testing tiles (13 positive, 11 negative; 5,570 frames per modality). In the held-out test split, each biome contributes exactly 8 tiles (desert: 4 positive, 4 negative; forest: 4 positive, 4 negative; snow alpine: 5 positive, 3 negative). Frames remain in exact chronological sequence to support causal temporal evaluation.

\section{Results and Discussion}
\label{sec:results}

\subsection{Upstream Detection Across Modalities and Biomes}
We first evaluate the upstream frame-level detection performance of the two fixed YOLO11n models and the late fusion max gate on the held-out test split under standard thresholding ($\tau = 0.50$). Table~\ref{tab:fusion_comparison} summarizes precision, recall, and F1-score across all three biomes.

\begin{table}[!t]
\centering
\caption{Frame-Level Upstream Detection Performance on Held-Out Test Split (Fixed Detectors, Standard Threshold $\tau = 0.50$)}
\label{tab:fusion_comparison}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Sensing Configuration / Environment} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
\midrule
YOLO11n-RGB (Arid Desert)          & 0.961 & 0.983 & 0.972 \\
YOLO11n-RGB (Temperate Forest)     & 0.884 & 0.835 & 0.859 \\
YOLO11n-RGB (Snow/Alpine)          & 0.938 & 0.882 & 0.909 \\
\midrule
YOLO11n-Thermal (Arid Desert)      & 0.892 & 0.965 & 0.927 \\
YOLO11n-Thermal (Temperate Forest) & 0.941 & 0.952 & 0.946 \\
YOLO11n-Thermal (Snow/Alpine)      & 0.865 & 0.914 & 0.889 \\
\midrule
\textbf{Late Fusion Max Gate (Arid Desert)}       & \textbf{0.912} & \textbf{0.995} & \textbf{0.952} \\
\textbf{Late Fusion Max Gate (Temperate Forest)}  & \textbf{0.895} & \textbf{0.988} & \textbf{0.939} \\
\textbf{Late Fusion Max Gate (Snow/Alpine)}       & \textbf{0.881} & \textbf{0.976} & \textbf{0.926} \\
\bottomrule
\end{tabular}%
}
\end{table}

The upstream modalities exhibit complementary physical failure modes: visible RGB provides sharp localization under open sunlight but suffers severe recall dropouts under dense forest canopy shadows (recall drops to 83.5\%); thermal infrared reliably recovers human heat signatures through foliage (recall 95.2\%) but incurs false positives on sun-heated desert rocks and elevated clutter in snow. The late fusion max gate ($s[n] = \max(c_{\text{RGB}}[n], c_{\text{Thermal}}[n])$) successfully bridges these unimodal blind spots, maximizing target recall to 99.5\% in desert, 98.8\% in forest, and 97.6\% in snow. However, because max-pooling forwards the highest confidence score, it also propagates isolated noise spikes from both sensors into downstream decision buffers, producing false alarms on negative distractor scenes. This motivates Rung 4 of the ladder: lightweight causal temporal filtering.

\begin{table*}[!t]
\centering
\caption{Comparative Benchmark of Causal Temporal Decision Methods on Held-Out Test Sequences ($W = 5$ frames / 208 ms at 24 Hz)}
\label{tab:postprocessing_benchmark}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
\textbf{Decision Method} & \textbf{Val $\tau^*$} & \textbf{Pos. Recall (\%)} & \textbf{Frame $F_1$-Score [95\% CI]} & \textbf{False Alerts / min} & \textbf{Alert Latency} & \textbf{Filter Runtime / Frame} \\
\midrule
M1: Baseline Raw Thresholding    & 0.43 & 98.5\%  & 0.848 [0.812, 0.884] & 12.0 & 0.0 ms (0 frames)  & 0.02 $\mu$s ($O(1)$, 0 B state) \\
\textbf{M2: Five-Frame Moving Average} & \textbf{0.57} & \textbf{100.0\%} & \textbf{0.971 [0.945, 0.992]} & \textbf{4.0} & \textbf{83.3 ms (2 frames)} & \textbf{0.30 $\mu$s ($O(1)$, 20 B state)} \\
M3: Five-Frame Median Filter     & 0.49 & 100.0\% & 0.877 [0.838, 0.914] & 4.0  & 83.3 ms (2 frames) & 0.42 $\mu$s ($O(W)$, 20 B state) \\
M4: Five-Frame History Consensus & 0.49 & 97.1\%  & 0.863 [0.821, 0.902] & 4.0  & 83.3 ms (2 frames) & 0.63 $\mu$s ($O(W)$, 5 B state) \\
M5: Learned Mamba-SSSM (5 seeds) & 0.73 & 100.0\% & 0.819 $\pm$ 0.018 [0.772, 0.861] & 6.0 $\pm$ 1.4  & 41.7 ms (1 frame)  & 150.59 $\mu$s ($O(d_{\text{model}}d_{\text{state}})$, 512 B) \\
M6: Learned GRU Baseline (5 seeds) & 0.50 & 100.0\% & 0.805 $\pm$ 0.021 [0.758, 0.852] & 8.0 $\pm$ 1.6  & 41.7 ms (1 frame)  & 21.80 $\mu$s ($O(d_{\text{hidden}}^2)$, 64 B) \\
\bottomrule
\end{tabular}%
}
\end{table*}

\begin{figure}[!t]
\centering
\resizebox{0.95\columnwidth}{!}{\input{figure_real_temporal_sequence.tex}}
\caption{Temporal sequence trace from an actual held-out test clip: (top) upstream fused confidence $s[n]$ exhibiting an isolated clutter spike at frame 18 followed by a sustained ground-truth target event from frame 32; (bottom) causal binary alert decisions comparing baseline thresholding M1 (spurious alert triggered at frame 18), five-frame moving average M2 (clutter spike completely suppressed, target confirmed with 2-frame latency), and Mamba-SSSM M5.}
\label{fig:real_temporal_sequence}
\end{figure}

\subsection{Comparative Evaluation of Causal Decision Methods}
Across the held-out test split, causal temporal post-processing dramatically suppresses false alarm events while preserving target recall. Table~\ref{tab:postprocessing_benchmark} reports the primary benchmark results across the six decision strategies, evaluated under validation-calibrated thresholds $\tau_m^*$ with 95\% confidence intervals across evaluation episodes. The five-frame moving average (M2) cuts false alerts by 67\% (from 12.0 down to 4.0 FA/min) and achieves an 87.0\% frame-level false alarm suppression ratio (FASR) while maintaining 100.0\% positive target recall and peak $F_1 = 0.971$ [0.945, 0.992] in just 0.30~$\mu$s execution time. Median filtering (M3) and history consensus (M4) similarly eliminate brief noise bursts shorter than 3 frames (4.0 FA/min), though discrete voting slightly clips target boundaries (97.1\% recall for M4). In contrast, learned sequence models do not outperform simple linear moving averages on this 1D scalar stream: M5 Mamba-SSSM yields $6.0 \pm 1.4$ FA/min ($F_1 = 0.819 \pm 0.018$) and M6 GRU yields $8.0 \pm 1.6$ FA/min ($F_1 = 0.805 \pm 0.021$). Because transient clutter spikes are isolated high-frequency impulses, parameterized recurrent networks retain hidden-state activation after a strong clutter impulse, prolonging false alert duration. Linear FIR averaging provides direct $1/W = 0.20$ attenuation of isolated noise pulses without state-transition delays. McNemar's test across all 5,570 test frames confirms that M2's reduction in false alarms over raw baseline M1 ($\chi^2 = 18.42, p < 0.001$), M5 ($\chi^2 = 12.65, p < 0.001$), and M6 ($\chi^2 = 14.89, p < 0.001$) is statistically significant.

\subsection{Environmental Robustness Across Biomes}
To examine whether temporal filtering performs consistently across diverse search environments, Table~\ref{tab:per_biome_benchmark} stratifies performance across the three held-out test biomes (8 tiles per biome, 5,570 frames total). Across all terrains, five-frame moving average (M2) substantially lowers false alert rates compared to instantaneous baseline thresholding (M1): in arid desert, false alerts drop from 10.50 to 1.50 FA/min (an 85.7\% reduction); in temperate forest, false alerts drop from 12.49 to 3.33 FA/min (a 73.3\% reduction); and in snow alpine terrain, false alerts drop from 4.50 to 1.50 FA/min (a 66.7\% reduction). While canopy occlusions in dense forest slightly reduce frame-level target overlap under heavy smoothing (alert recall 75.0\% for M2 vs. 100.0\% for order-statistic median M3), M2 achieves the lowest false alert rate across all three biomes, confirming robust environmental transferability.

\begin{table}[!t]
\centering
\caption{Environment-Stratified Causal Decision Performance on Held-Out Test Split (8 Tiles per Biome, 5,570 Total Frames)}
\label{tab:per_biome_benchmark}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
\textbf{Environment} & \textbf{Decision Method} & \textbf{Val $\tau^*$} & \textbf{Alert Recall (\%)} & \textbf{FA / min} & \textbf{FP Frames} \\
\midrule
\textbf{Arid Desert} 
  & M1: Raw Baseline        & 0.43 & 100.0\% & 10.50 & 32 \\
  & M2: Moving Average      & 0.57 & 100.0\% & \textbf{1.50} & \textbf{10} \\
  & M3: Median Filter       & 0.49 & 100.0\% & 3.00 & 20 \\
  & M4: History Consensus   & 0.49 & 100.0\% & 3.00 & 20 \\
\midrule
\textbf{Temperate Forest} 
  & M1: Raw Baseline        & 0.43 & 100.0\% & 12.49 & 34 \\
  & M2: Moving Average      & 0.57 & 75.0\%  & \textbf{3.33} & \textbf{13} \\
  & M3: Median Filter       & 0.49 & 100.0\% & 4.99 & 21 \\
  & M4: History Consensus   & 0.49 & 100.0\% & 4.99 & 21 \\
\midrule
\textbf{Snow/Alpine} 
  & M1: Raw Baseline        & 0.43 & 80.0\%  & 4.50 & 9 \\
  & M2: Moving Average      & 0.57 & 80.0\%  & \textbf{1.50} & \textbf{3} \\
  & M3: Median Filter       & 0.49 & 80.0\%  & 3.00 & 10 \\
  & M4: History Consensus   & 0.49 & 80.0\%  & 3.00 & 10 \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[!t]
\centering
\resizebox{0.86\columnwidth}{!}{\input{figure_window_ablation.tex}}
\caption{Temporal window length ablation ($W \in \{1, 3, 5, 7\}$) for the moving average filter on the held-out test split, showing $F_1$-score ($\uparrow$) and False Alerts/min ($\downarrow$) across causal latency bounds (42~ms to 292~ms at 24~Hz). $W = 5$ achieves peak $F_1$ (0.971) and minimizes false alarms.}
\label{fig:window_ablation}
\end{figure}

\subsection{Temporal Window Length Sensitivity}
A window of $W = 5$ frames ($208\text{ ms}$) achieves the optimal operating trade-off, reaching peak $F_1 = 0.971$ and cutting false alerts to 4.0 FA/min before latency degradation emerges (Fig.~\ref{fig:window_ablation}). Ablating $W \in \{1, 3, 5, 7\}$ frames ($41.7\text{ ms}$ to $291.7\text{ ms}$ at $24\text{ Hz}$) shows that $W = 1$ collapses to baseline raw thresholding ($F_1 = 0.848$, 12.0 FA/min). Expanding to $W = 3$ smooths high-frequency noise, increasing $F_1$ to 0.925 and halving false alerts to 6.0 FA/min. At $W = 5$, the filter reaches peak performance ($F_1 = 0.971$, 4.0 FA/min, 100.0\% recall) within a 208~ms latency bound. Expanding further to $W = 7$ delays target onset detection, creating lag and lowering $F_1$ to 0.942. Thus, $W = 5$ provides the optimal operational balance.

\subsection{Limitations}
\label{sec:limitations}
Several constraints bound our findings. First, pseudo-thermal infrared streams are generated via a global grayscale lookup table rather than radiometric sensor physics; high-albedo snow renders as the warmest region in frame, inverting thermal contrast relative to physical snowpack. Second, the benchmark evaluates 24 held-out test tiles (5,570 frames per modality under Option B parent-video grouping, 8 tiles per biome with 13 positive and 11 negative tiles); broader distractor balance across maritime and urban domains remains future work. Third, clips capture nominal aerial trajectories ($30$--$50$\,m AGL at $8$--$15$\,m/s) in clear conditions; violent turbulence and degraded weather (heavy fog, blizzard snowfall) require further study. Finally, timing benchmarks reflect desktop GPU execution; embedded Jetson Orin Nano flight validation is planned for subsequent work.

\section{Conclusion}
\label{sec:conclusion}
This paper investigated lightweight multimodal person detection with causal temporal post-processing for UAV wilderness SAR across desert, forest, and snow biomes. Holding upstream YOLO11n detectors and the late fusion max gate fixed, we benchmarked six causal decision methods under validation-calibrated thresholds. When the upstream multimodal detector already produces an informative scalar confidence stream, a short causal moving average delivers the best operational trade-off (100\% target recall, 67\% false-alert reduction down to 4.0 FA/min) at negligible computational cost (0.30~$\mu$s runtime, 20-byte buffer). More complex recurrent or state-space models add latency and parameters without improving alert reliability on this task. Causal temporal post-processing provides an essential, computationally negligible layer for preventing wasteful UAV loiters and conserving bandwidth in autonomous search operations.

\balance

\section*{AI Use Disclaimer}
Generative artificial intelligence (AI) tools were used in this research for synthetic training video generation (Google Gemini 3.8 Flash and xAI Grok) as well as AI-assisted manuscript editing and proofreading. All experimental designs, model training, algorithmic code, post-processing pipelines, quantitative evaluations, and scientific interpretations were developed and verified by the authors.

\section*{Data and Code Availability}
Simulation frameworks, snippet harvesting tools, dataset split manifests, and PyTorch temporal decision implementations are available at: \url{https://github.com/IamOumarIbrahim/multi-modal-detection}.

\section*{Acknowledgment}
The authors acknowledge the Department of Computer Engineering at the University of Sharjah for supporting this research and providing computing facilities.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}